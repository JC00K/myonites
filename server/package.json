{
  "name": "@myonites/server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "eslint api/",
    "test": "vitest run",
    "clean": "rm -rf .vercel .turbo"
  },
  "dependencies": {
    "@myonites/shared": "workspace:*",
    "@supabase/supabase-js": "^2.47.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "eslint": "^9.39.3",
    "typescript": "^5.6.0",
    "vercel": "^39.0.0",
    "vitest": "^2.1.0"
  }
}
